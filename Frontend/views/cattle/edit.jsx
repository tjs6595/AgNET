const React = require('react')
const Def = require('../default')

// Pass in new data
function editForm (data) {
    return (
        <Def>
            <main>
                <h1>Edit Cattle </h1>
            </main>
        </Def>
    )
}

module.exports = editForm
